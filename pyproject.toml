[project]
name = "mystery-agent-lab"
version = "0.1.0"
description = "AI murder mystery agent lab"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.7,<3.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]